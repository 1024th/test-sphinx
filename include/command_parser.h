#include <iostream>
#include <string>

/**
 * @brief 解析输入指令。
 *
 */
class CommandParser {
 public:
  /**
   * @brief 循环读入指令并解析，直到遇到 exit。
   *
   */
  void Run();
  /**
   * 这是 Add 的注释
   * @param x 这是对参数 x 的注释
   * @return 这是对返回值的注释
   */
  int Add(int x);
};
